# v0.1 Milestone: One Real Audited System Recovery Loop

## Goal

Build the smallest version of Runlane that can handle one real or simulated system-level incident across Linux, FreeBSD, and OpenBSD, with audited evidence collection, dependency-aware scheduling, impact-scoped verification, human approval, and a privileged recovery action.

## Acceptance criteria

- A server can enroll a node and issue pull tasks.
- Linux, FreeBSD, and OpenBSD agents can report platform capabilities.
- The agent can collect service status, logs, disk, process, and socket facts through platform-native backends or clearly report unsupported capabilities.
- A runbook from a Git fleet repo can be selected and executed.
- Tasks declare resources they read/write and scheduler respects dependency/conflict constraints.
- A verification planner selects checks based on impact scope instead of running a global full gate.
- A model or deterministic stub analyzer can produce a structured proposal.
- A privileged action requires approval and a signed capability lease.
- The local helper refuses invalid, expired, replayed, or locally disallowed leases.
- Every state transition, lease decision, verification decision, skipped check, and action result writes an audit event.
- A final incident report / cognitive receipt can be generated from the ledger.

## Phase 0: project definition

- [x] Adopt project name: Runlane.
- [x] Define product identity and non-goals.
- [x] Define cross-platform stance.
- [x] Define relevance-scoped verification requirement.
- [x] Define dependency/conflict-aware concurrency requirement.
- [x] Define system-layer dogfood scope.
- [ ] Create GitHub repository.

## Phase 1: core domain and state machine

- [ ] Define `Node`, `Platform`, `Capability`, `Resource`, `ResourceLease`, `ImpactSet`, `VerificationPlan`, `Runbook`, `Incident`, `Run`, `Task`, `Evidence`, `Proposal`, `Approval`, `CapabilityLease`, `AuditEvent`.
- [ ] Implement run state transition validation.
- [ ] Implement task DAG model.
- [ ] Add tests for legal and illegal transitions.
- [ ] Add tests for resource lease compatibility.

## Phase 2: scheduler and scoped verification

- [ ] Build in-memory scheduler that respects explicit dependencies.
- [ ] Add resource conflict detection.
- [ ] Add lease modes: `observe`, `intent`, `exclusive`, `drain`, `reboot`.
- [ ] Add verification planner that maps `ImpactSet` to checks.
- [ ] Add audit events for checks selected and checks skipped.
- [ ] Test that unrelated checks are not selected for simple service restart.
- [ ] Test that broad checks are selected for package/reboot-class operations.

## Phase 3: agent pull loop

- [ ] Implement node config loading.
- [ ] Implement enrollment token bootstrap.
- [ ] Implement mTLS identity storage.
- [ ] Implement `GET /agent/tasks/next` pull endpoint.
- [ ] Implement task result submission.
- [ ] Add local spool for failed submissions.
- [ ] Add replay protection for task/action messages.

## Phase 4: platform backends

- [ ] Define `PlatformBackend` trait and driver families.
- [ ] Implement Linux capability reporting and skeleton collectors.
- [ ] Implement FreeBSD capability reporting and skeleton collectors.
- [ ] Implement OpenBSD capability reporting and skeleton collectors.
- [ ] Add fixture tests for platform parser outputs.
- [ ] Ensure unsupported capabilities fail closed with clear reasons.

## Phase 5: fleet repo sync

- [ ] Define fleet repo layout.
- [ ] Parse inventory YAML.
- [ ] Parse role YAML.
- [ ] Parse runbook YAML.
- [ ] Parse policy YAML.
- [ ] Resolve overlays: global -> OS -> role -> environment -> node.
- [ ] Add `runlane server gitops sync` command.

## Phase 6: approval and helper

- [ ] Define typed privileged actions.
- [ ] Implement capability lease signing and verification.
- [ ] Implement nonce/replay rejection.
- [ ] Implement helper CLI shape: `runlane-helper execute --lease-file ... --request-file ...`.
- [ ] Add local helper allowlist config.
- [ ] Add sudo/doas install docs for Linux, FreeBSD, OpenBSD.
- [ ] Test invalid lease rejection.
- [ ] Test expired lease rejection.
- [ ] Test action-target mismatch rejection.
- [ ] Test local allowlist rejection.

## Phase 7: analyzer

- [ ] Start with deterministic rule-based analyzer for v0.1 demo.
- [ ] Add optional LLM analyzer behind a feature flag/config flag.
- [ ] Require structured proposal output.
- [ ] Validate proposals against policy before approval.
- [ ] Add prompt-injection fixtures in logs to verify they are treated as data.

## Phase 8: approval adapters

- [ ] Implement CLI approval first.
- [ ] Add Telegram approval adapter second.
- [ ] Ensure Telegram calls the same approval API as CLI.
- [ ] Keep bot logic node-agnostic.

## Phase 9: demo

- [ ] Create `examples/fleet` with Linux, FreeBSD, and OpenBSD nodes.
- [ ] Create `service-unhealthy` runbook.
- [ ] Simulate a service failure.
- [ ] Collect evidence.
- [ ] Generate proposal.
- [ ] Approve restart.
- [ ] Execute helper action.
- [ ] Run impact-scoped verification.
- [ ] Generate incident report.

## v0.1 non-negotiables

- No arbitrary shell action.
- No direct model-to-command path.
- No systemd-only design.
- No permanent broad sudo/doas.
- No chat-bot-specific business logic.
- No per-node repository requirement.
- No blanket full-gate after every small task.
- No unmodeled concurrent mutation.

## Kill criteria

Stop or redesign if, after v0.1:

- it is not useful on the operator's own machines;
- onboarding FreeBSD/OpenBSD feels bolted on;
- relevance-scoped verification is not explainable;
- concurrency is still all-serial or unsafe;
- approval/audit is decorative rather than enforcing behavior;
- the project drifts into a generic agent dashboard;
- Git intent and runtime state become confused again.
